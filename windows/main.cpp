#include <iostream>
#include <string>

int	main()
{
	std::string str;
	std::cout << "Hello, the zia server is not avaible now on windows please wait portability" << std::endl;
	std::cin >> str;
	return (0);
}